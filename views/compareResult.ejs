<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Результат порівняння відеокарт</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }
    th, td {
      border: 1px solid black;
      padding: 8px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <h1>Результат порівняння відеокарт</h1>

  <table>
    <tr>
      <th>Назва</th>
      <th>Пам'ять (GB)</th>
      <th>Частота ядра (MHz)</th>
      <th>Ціна ($)</th>
    </tr>
<% for (let i = 0; i < cardsToCompare.length; i++) { %>
  <tr>
    <td><%= cardsToCompare[i].name %></td>
    <td><%= cardsToCompare[i].memory %></td>
    <td><%= cardsToCompare[i].clockSpeed %></td>
    <td><%= cardsToCompare[i].price %></td>
  </tr>
<% } %>

  </table>

  <h2>Відеокарта "<%= betterCard.name %>" краща</h2>

</body>
</html>
